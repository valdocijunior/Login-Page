import React from 'react';
import { ButtonContainer, LoginButton } from './style';

function Button() {
  return (
    <ButtonContainer>
        <LoginButton>Login</LoginButton>
    </ButtonContainer>
  )
}

export default Button;
